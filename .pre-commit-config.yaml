---
repos:
  - repo: local
    hooks:
      - id: ansible-lint-local
        name: ansible-lint (local venv)
        entry: .venv/bin/ansible-lint
        language: system
        pass_filenames: true
        files: \.(ya?ml)$
        exclude: ^(\.venv/|\.git/|\.molecule/|__pycache__/)
